mod ast;
mod scanner;
mod parser;
mod symbols;
mod lowering;
mod table;
mod generator;

use crate::ast;
use crate::scanner;
use crate::parser;
use crate::symbols;
use crate::lowering;
use crate::table;
use crate::generator;

fn main() {
    println!("Hello, world!");
}
