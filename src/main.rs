mod ast;
mod scanner;
mod parser;
mod symbols;
mod lowering;
mod table;

use crate::ast;
use crate::scanner;
use crate::parser;
use crate::symbols;
use crate::lowering;
use table;

fn main() {
    println!("Hello, world!");
}
