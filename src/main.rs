mod ast;
mod scanner;
mod parser;
mod symbols;
mod lowering;

use crate::ast;
use crate::scanner;
use crate::parser;
use crate::symbols;
use crate::lowering;

fn main() {
    println!("Hello, world!");
}
